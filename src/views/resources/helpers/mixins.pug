mixin container()
	.container&attributes(attributes)
		block

mixin image()
	-
		let src = attributes.src.split('.')
		let type = src.pop()

		src = src.join('.')
		delete attributes.src

	picture
		source(srcset=`${src}.webp` type='image/webp')
		img(src=`${src}.${type}`)&attributes(attributes)